import { defineConfig } from 'vite'

// https://vitejs.dev/config/
export default defineConfig({
  // No special plugins needed for this simple setup
  // React plugin is often used, but for this basic build, it's not strictly necessary
  // as long as we handle JSX correctly in tsconfig.
})
